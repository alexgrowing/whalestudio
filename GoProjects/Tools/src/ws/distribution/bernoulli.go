package distribution

// BernoulliCalc BernoulliCalc
// func BernoulliCalc(n int, p float64) {
// 	for k := 0; k <= n; k++ {
// 		fmt.Printf("事件%2d次发生的概率是%.6f\n", k, kCalc(n, p, k))
// 	}
// }

// func kCalc(n int64, p float64, k int64) float64 {
// 	return Combine(n, k) * math.Pow(p, float64(k)) * math.Pow(1-p, float64(n-k))
// }

//  看一下桌面上的sqrt.xls，自动算出不同调研样本得到结果的置信度就可以了，现在上面写的两个方法，可能用不到了
