当App将要Enter Foreground的时候,读取剪贴板的内容,提示要不要新建一个Card
当App自己里面复制了内容,还要提示吗?
TextKnowledge和ImageKnowledge要分开来，没必要相互切换
ImageKnowledge没必要支持编辑
KCEditViewController在新增和编辑的时候要区分一下，或者，只要一新建，那么就在KCMain里面新增了一个Card，在KCEdit里面就只能编辑这个卡片的内容，要删除，请出去删除，完美

// 把卡片弄成层叠的结构，一张张地看过去，不要用scrollview的模式

// 如果是图片类型的卡片,不要直接存UIImage到Defaults吧,单独把图片放在哪个地方,然后保存路径吧,或者看看为什么有的时候选择一张图片后会卡死

// ShuffleViewController点击一下能进入全屏模式，下面的Toolbar能慢慢地褪掉，最好默认Toolbar也是带有一定的透明度
// ShuffleViewController右上角的label位置不好
ShuffleViewController里面的Image可以放大缩小查看
// 把编辑和删除移到ShuffleViewController里面

// 在Shuffle时删除卡片，回到ThumbnailView要reloadData
// 上一页下一页的翻页效果
评论
// 左划，把前面一张卡片拿上来吧
// 滑动时候可以显示当前是第几页，或者当前是第几张卡片
在Start页面可以筛选，筛选后再Shuffle就是Shuffle筛选的内容
如果从某个地方拷贝了文本，打开app时是在shuffle页，怎么办？
分多页的Text，第一页的最后一行可能没有满
// 删除一张卡片的时候，加载顺序不要弄乱了
划到第一张的时候，再左划的话，要找个地方提示一下，说明前面没有了已经是第一张了
把Shuffle的顺序重新整理一下吧，现在倒过来，总觉得不舒服
即使没有登录，也可以统计一下有点操作的次数吧，比如直接粘贴的次数，新建文本的次数，新建图片的次数，相机的次数等等
// 每次QuickLogin之后，也可以把过期时间往后面延一延
先做搜索吧，搜索出来的结果集，然后Shuffle就基于这个结果集，顺便把Shuffle的序号倒序的问题也解决掉
// 翻到最后一页，那个页码不要+1了
// 现在这种序号模式下，删除卡片不知道有没有问题


// 为什么如果原来首页是空白，登录之后，还是空白的
// 而且图片也加载不出来
退出之后，回到首页，怎么还是显示同步的状态
// 往前面翻页，翻页之后，还会再有一次翻页效果，这个不行啊
// 居然服务器上有些图片丢了，重新传上去吧，也不知道怎么丢掉了
统计一下服务器上现在有多少图片，容量又是多少
上传的时候也看不出来正在上传，怎么这么多问题啊，之前好像都测过的呀
登录下载的时候还有点卡么
// 妈蛋，看起来，从手机上传图片到服务器，总是不成功啊，要在localhost服务器上先测一下（还真是没法上传成功，NND）
图片在下载的时候，不管是thumb还是大的卡片，都显示【等待下载】【下载中】【下载完成】如果能像石墨一样显示【当前下载进度】就更好
图片在上传的时候，能不能显示【等待上传】【上传中】【上传完成】，如果能显示【当前上传进度】就更好
// 如果是图片卡片，先把图片上传之后，再上传配置文件吧，不然有可能导致下载到的图片卡片都看不到东西
上传图片，跟下载图片一样，也弄个Queue吧
上传图片和下载图片的Queue，最好有地方可以查看得到
可以同时上传多个图片
换成默认用GameCenter的帐号或者自动生成一个帐号吧，非要用户注册帐号才能上传服务器，对我这个产品不好啊
确认一下，首页点击thumb进入的是order的查看模式，点击shuffle进入shuffle模式，两者都显示搜索后的结果吗？？？要想一下
搜索界面进入order的shuffle模式，然后返回，最好还是在搜索出来的几个Card吧



在AppDelegate开始的时候，已经readUserPasscode，那么，如果passcode有效的话，是不是就是直接登录了呀，那这里再登录GameCenter，有可能会冲突，有可能不冲突，怎弄
统计一下知识卡片的使用情况吧
--24小时新增用户数
--24小时使用用户数
--topN用户简介
